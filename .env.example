# bắt buộc (Cloud Run có thể set bằng --set-env-vars hoặc Secret Manager)
VPS_BASE=
VPS_BEARER=
CALLBACK_BEARER=

# Bật/tắt LLM (Gemini)
ENABLE_LLM=false
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GEMINI_BASE=https://generativelanguage.googleapis.com/v1beta

# SQLite path (Cloud Run ghi được /tmp)
DB_PATH=/tmp/db.sqlite
